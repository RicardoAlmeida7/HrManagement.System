@page "/company/employee/delete"
@model HrManagement.WebApplication.Pages.Company.Employee.DeleteModel
@{
    Layout = "_LayoutModal";
}
<form asp-page="/company/employee/delete" method="post" onsubmit="return jQueryAjaxPost(this)">
    @Html.HiddenFor(model => model.Employee.Id)
    <div>
        <h2>
            <strong>
                Deseja excluir permanetemente o colaborador(a) @Model.Employee.Name?
            </strong>
        </h2>
        <p class="alert alert-warning text-danger text-center">
            <strong>
                Esta ação não poderá ser desfeita após a confirmação!
            </strong>
        </p>
    </div>
    <div class="form-group" style="text-align:right">
        <input type="button" value="Cancelar" data-bs-dismiss="modal" class="btn btn-outline-secondary" />
        <input type="submit" value="Confirmar" class="btn btn-primary" style="margin-left:10px" />
    </div>
</form>
